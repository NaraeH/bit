이 디렉토리는 java002 프록젝트에서 사용하는
외부 라이브러리 파일(.jar)을 두는 디렉토리이다.

이 디렉토리에 있는 JAR(Java Archive(묶음))파일을 사용하려면 
프로젝트의 classpath에 등록해야 한다.

프로젝트 속성창 > Java Build Path > Libraries에서
[Add JARs] 버튼 클릭하여 해당 JAR파일을 등록한다.